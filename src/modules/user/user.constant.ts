export enum USER_STATUS {
  ACTIVE = 'active',
  DEACTIVE = 'deactive',
}

export const USERNAME_MAX_LENGTH = 12;

export enum USER_ORDER_COLUMN {
  CREATED = 'createdAt',
  NAME = 'name',
  EMAIL = 'email',
}
